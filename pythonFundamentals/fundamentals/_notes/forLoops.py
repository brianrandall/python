groceryList = ['apples', 'eggs', 'coffee', 'meat', 'cheese', 'potatoes', 'salmon']

for item in groceryList:
    print(item)

#range takes 3 parameters - start, stop, step (increment amount)
#start - optional (default 1)
#stop - required (num)
#step - optional (default 1)

#  define variable (index)
for index in range(len(groceryList)):
    print(index)

# do the same thing for dictionaries
for key in dadcup:
    print(dadcup[key])